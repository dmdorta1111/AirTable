# =============================================================================
# Meilisearch Service for PyBase
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: pybase-meilisearch
  namespace: pybase
  labels:
    app.kubernetes.io/name: pybase
    app.kubernetes.io/component: meilisearch
    app.kubernetes.io/part-of: pybase
spec:
  type: ClusterIP
  ports:
    - port: 7700
      targetPort: 7700
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: pybase
    app.kubernetes.io/component: meilisearch
